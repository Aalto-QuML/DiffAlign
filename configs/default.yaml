defaults:
    - general : default
    - diffusion : discrete
    - neuralnet: transformer
    - train : default
    - test : default
    - dataset : uspto-50k
    - override hydra/job_logging: none
    - _self_

hydra:
    job:
        chdir: 'True'
    run:
        dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}-${general.name}


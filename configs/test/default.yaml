with_denoising: True
smiles_accuracy: True
chains_to_save: 1
size_test_splits: 100
batch_size: 128
n_samples: 1
condition_first: 0 # The start of the overall condition range in a sampling run
condition_index: 0 # the index of a condition range in the current job, in relation to condition_first. See sample_array_job for details
condition_steps: null
n_conditions: 1
n_samples_per_condition: 1
wandb_log_samples: True
full_dataset: False
testfile: None
topks: [1, 3, 5, 10, 50, 100]
loss_0_repeat: 1
repeat_elbo: 1
elbo_samples: 2
eval_before_first_epoch: False
plot_dummy_nodes: True
plot_rxn_chains: True
wandb_run_name: ''
wandb_run_config: ''
sort_lambda_value: 0.9 # 1 corresponds to no effect from the counts, 0 corresponds to no effect from ELBO when sorting
total_cond_eval: null
inpaint_on_one_reactant: False
product_smis_list: ["[NH:1]1[CH2:2][CH:3]([N:4]2[CH2:5][CH:6]([OH:7])[CH2:8]2)[CH2:9]1"]
num_samples_per_condition_subrepetitions: [100] # e.g., [100,50,25,8] -> gives the batch size when evaluating the samples_per_condition
num_samples_per_condition_subrepetition_ranges: [] # e.g., [10,20,50] -> gives the cutoffs (in terms of graph node count) of which batch size to use when evaluating the samples_per_condition
return_smiles_with_atom_mapping: False
reassign_atom_map_nums: False
eval_elbo_during_training: True
